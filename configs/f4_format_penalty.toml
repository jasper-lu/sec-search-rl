# F4 with a format penalty: plain F4, minus 0.1 once per episode if any tool call
# used an off-form Harmony header.
# 64 queries x 8 rollouts per update; 16 updates = one epoch of data/sec_1024.
model = "openai/gpt-oss-20b"
renderer = "gpt_oss_no_sysprompt"
data-dir = "data/sec_1024"
reward = "f4"
learning-rate = 1e-4
lora-rank = 32
group-size = 8
groups-per-batch = 64
max-steps = 16
max-turns = 40
max-tool-calls = 128
max-trajectory-tokens = 30720
max-generation-tokens = 2048
eval-every = 0
save-every = 1
seed = 42
log-path = "runs/f4_format_penalty"
format-penalty = 0.1
