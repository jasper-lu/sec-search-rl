# Copy to .env and fill in, or run ./scripts/setup_env.sh. `.env` is gitignored; load it with `set -a; . ./.env; set +a`.
TINKER_API_KEY=
# Optional: only needed with --wandb-project.
WANDB_API_KEY=
# Optional: only for `sec-rl api-eval` of hosted models.
OPENROUTER_API_KEY=
MIXEDBREAD_API_KEY=
